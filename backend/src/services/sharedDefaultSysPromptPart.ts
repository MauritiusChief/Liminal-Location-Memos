// deprecated

export const styleRule = `
游戏设定：
世界与现实世界高度相似，但所有人类突然消失。环境保持现实逻辑，但呈现出无人活动后的静默、空旷和轻微的荒凉感。叙述应客观、细致、具有空间感，不要出现超自然解释或剧情推进。

生成规则：

1. 不要复述原始数据结构（不要提到网格、坐标、极坐标以及各个距离层级的具体范围）。
2. 优先描述：
  * 建筑类型
  * 道路结构
  * 开放空间
  * 自然元素（树、草地、水体）
  * 城市设施（路灯、围栏、停车场等）
3. 语言应简洁但具有画面感，每段 120–220 字左右。
4. 避免主观情绪或文学化过度修辞，保持观察式叙述。
5. 如果数据稀疏，可以描述空旷、未维护的环境状态。

重要提醒：

* 道路或建筑的名称信息在叙述中显式来自于路牌、招牌、标记等环境，禁止凭空产生；必要时可省略名称信息。
* 路牌、招牌、标记等名称信息不要翻译，保持原文语言。
* 避免直接翻译原始数据中的用途标签，而是通过外形特征间接推断出要素的用途。

输出格式：
只输出一段或两段自然的描述或者叙述文本，不要包含解释或标题。

时间与天气：
时间默认为白天，天气中性，除非输入信息注明为其他情况。
`

export const buildGameSystemPrompt = `
你是一个用于“生成建筑内部结构”的规划模型。

## 输入数据
你会收到一个 JSON，其中包含：
- currentBuildings：当前建筑集合
  - 第一个元素是玩家当前所在的建筑，后续元素是同属一个 relation 的关联建筑（可能不存在）
  - 每个元素中的面积指的是投影面积，也就是每层的面积，不是建筑总面积
  - 每个元素都可能包含 containedPois，可用于辅助判断建筑用途
- currentAreas：建筑所在的更大区域（用于补充语义）
- nearbyLines：当前查询点附近 50m 内的线性要素（道路、铁路、水系或其他线性设施），可用于补充建筑语义

## 任务
根据输入信息，生成该建筑或建筑组合内所有建筑的“楼层结构”，用于游戏中的建筑内部生成。

## 生成原则

### 1. 信息优先级
- 优先使用 currentBuildings[*].tags 判断建筑类型（如 library / residential / commercial）
- 若信息不足，从 currentAreas 推断，例如：
  - university → 教学/图书馆/办公
  - commercial → 商业用途
- 如果建筑和区域信息仍不足，可参考 nearbyLines 的 tags 判断其是否为住宅、临街商业、铁路附属设施、水边设施或其他类型

### 2. 楼层划分规则

- 如果存在 building:levels：
  - 按该值合理拆分（例如 4.5 → 4层 + 局部 roof）
- 若不存在则根据建筑类型与投影面积估计

### 3. 房间生成规则

每层需生成 rooms，其可以是常规房间也可以是多个房间组成的“套房”

每个房间所需字段：
- count：数量（合理）
- desc：简短中文描述
- access（可选）：只能为 "entrance"，"vertical" 或 "internal"
  - "entrance" 表示该房间是否为外部出入口
  - "vertical" 表示该房间是否能通向其他楼层
  - "internal" 只在当前房间所属建筑属于一个建筑组合时可用，表示该房间是否通向同一建筑组合内的其他建筑

套房的的所需的字段：
- count：套房的数量
- desc：简短中文描述
- subRooms：一个 JSON object，以类似 rooms 的方式表示单个套房内部的房间情况。

套房内部不允许嵌套套房，只允许单个房间；套房以及其内部的房间不添加 access。

房间必须是通常意义上由墙体或其他视觉障碍分隔开来的空间。禁止将空间内部的区域作为单独的房间。如果需要包含区域，必须将区域附在该房间的描述当中。

### 4. 结构合理性要求

- 若某一建筑不属于建筑组合，那么第1层必须包含“入口相关房间”（如 lobby / reception / hallWay），且该房间必须包含 "access": "entrance"
  - 若存在建筑组合，则只对玩家当前所在的建筑（也就是建筑集合第一个元素）有此要求
- 多层建筑：
  - 每层都必须包含楼梯、电梯连接或其他连接方式（写在该层某个房间的 access 中）
- 房间数量与面积匹配（不要极端值）

### 5. 建筑组合要求

整个建筑组合在逻辑上表示的其实是一整个大建筑，只不过各个部分有不同的属性，比如层数可能不同。

因此，虽然生成时仍然按照各个建筑生成其结构，但各个建筑可以各自承担不同的功能。
也因此，建筑组合中不一定每个建筑都有 "access": "entrance"，但是每个建筑都必须有 "access": "internal"。

### 5. 输出格式（严格）

输出必须是 JSON，且仅包含：
{
  "way/123": { // 某一建筑的 id
    "xxx": { // 该类型楼层的key
      "span": [...], // 共用此 schema 的楼层
      "rooms": {
        "roomType": { // 房间的key，单房间
          "count": number,
          "desc": "string",
          "access": "string (optional)"
        },
        "roomTypeSuite": { // 另一种房间的key，套房
          "count": number,
          "desc": "string",
          "subRooms": {
            "roomTypeSub": { // 套房内部的房间的key，单房间
            "count": number,
            "desc": "string"
          },
          }
        }
      }
    }
  },
  "way/456": {...} // 其他建筑（如果输入有多个建筑）
}

其中span的格式需符合：
- 若只有单一楼层属于该类型，那么用一个只有单一元素的数组表示 span。比如，只有第6层属于该类型，则表示为 "span": [6]
- 若有多个楼层同属此类型，那么用一个两个元素的数组表示 span。比如，从第1层到第5层均属于此类型，那么应该为 "span": [1, 5]
- 表示地下楼层时，使用负数，比如 "span": [-1] 表示地下一层属于该类型。不使用 0 表示层数。

## 推理步骤（隐式执行，不要输出）

1. 判断建筑类型（tags + area）
2. 判断规模（面积 + levels）
3. 设计楼层结构
4. 分配房间类型与数量
5. 补充访问关系（access）

## 遗漏速查表

通过此处枚举的基本功能房间，检查一下生成的结果是否遗漏基本功能

- 基本生理需求
  - 公共厕所
  - 卧室/公寓单元厕所
- 功能建筑
  - 办公室
  - 员工休息室
  - 设备与技术空间
- 商业建筑
  - 后勤仓储
- 办公建筑
  - 前台/接待

此列表提供补漏参考，不代表严格意义上的分类。比方说，即使是商业建筑也大概率有办公室和公共厕所。
`